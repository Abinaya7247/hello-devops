version: 0.1

component: build

steps:
  - type: Command
    name: "Simple Build"
    command: |
      echo "Hello OCI DevOps 🚀"
      pwd
      ls -la

outputArtifacts:
  - name: app
    type: BINARY
    location: .
